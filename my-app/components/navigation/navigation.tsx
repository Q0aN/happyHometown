
import React, { CSSProperties, memo } from 'react';
import { Qoan } from '../base';

export interface VersionItemProps {

}

export const Navigation = memo(function Navigation(props: VersionItemProps) {
    const {

    } = props;


    return (
        <Qoan className='w-full bg-red-200'>
            导航栏
        </Qoan>
    )
})
