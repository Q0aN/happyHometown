
const appPage = 'fixed inset-0 overflow-hidden ' 
const appPageCanvas = 'absolute inset-0 w-full h-full block '
const appPageBoard = 'absolute top-8 left-1/2 transform -translate-x-1/2 z-10 '
const appPageBoardText = 'text-slate-600 text-2xl font-light opacity-80 '

export const appPageStyle = {
    base:appPage,
    canvas:appPageCanvas,
    board:appPageBoard,
    boardText:appPageBoardText
}